# Copilot Playground

一款用于通过 GitHub Copilot API 测试和比较 GitHub Copilot Pro 模型的渐进式 Web 应用（PWA）。

[English](./README.md) | 中文

## 功能特性

- 🔐 **GitHub 身份验证** — 设备流 OAuth 或个人访问令牌
- 🤖 **模型浏览器** — 列出所有 Copilot 模型，包含等级（高级/标准）、上下文窗口大小及每月请求配额
- 💬 **聊天界面** — 与任意 Copilot 模型进行流式聊天，支持对话历史、系统提示预设及可调节的温度/最大令牌数
- 🔄 **模型对比** — 同时向两个模型发送相同的提示词
- 📱 **PWA** — 可安装，支持离线使用（缓存后）

## 快速开始

### 1. 前置条件

- 拥有有效 **GitHub Copilot Pro**（或更高版本）订阅的 GitHub 账号
- Node.js 18+ 及 npm

### 2. 安装与运行

```bash
npm install
npm run dev
```

然后在浏览器中访问 http://localhost:5173。

### 3. 身份验证

#### 方式 A：个人访问令牌（推荐 — 最简单）

1. 访问 https://github.com/settings/tokens/new?scopes=read:user&description=CopilotApp
2. 生成一个具有 `read:user` 权限的令牌
3. 将其粘贴到应用的"使用个人访问令牌"表单中

#### 方式 B：GitHub 设备流 OAuth

1. [创建 GitHub OAuth 应用](https://github.com/settings/applications/new)：
   - **应用名称**：`Copilot Playground`（或任意名称）
   - **主页 URL**：`http://localhost:5173`（或您部署的应用 URL）
   - **应用描述**：*（可选）* 例如 `GitHub Copilot Playground`
   - **授权回调 URL**：`http://localhost:5173`（设备流不使用回调，但 GitHub 要求填写一个有效 URL）
   - 勾选 **"启用设备流（Enable Device Flow）"**
   - 点击 **注册应用** 并记录您的 **Client ID**
2. 在应用中输入 Client ID，然后点击 **获取设备码**
3. 访问 `github.com/login/device` 并输入显示的验证码

> **注意：** Copilot API（`api.githubcopilot.com`）在从浏览器直接调用时可能存在 CORS 限制。如遇到 CORS 错误，可将应用部署在本地服务器上，或使用浏览器扩展在开发环境中放宽 CORS 限制。

### 4. 使用 Docker 运行

```bash
docker build -t copilot-app .
docker run -p 8080:80 copilot-app
```

然后在浏览器中访问 http://localhost:8080。

> **注意：** 无需配置环境变量 — 所有 API 调用和身份验证均在浏览器端完成。

### 5. 构建生产版本

```bash
npm run build
npm run preview
```

`dist/` 文件夹是一个完全静态的 PWA，可部署到任何平台（Vercel、GitHub Pages、Netlify 等）。

## 模型等级

| 等级 | 说明 |
|------|------|
| **高级（Premium）** | 消耗每月请求配额（Copilot Pro：每月 300 次） |
| **标准（Standard）** | 活跃订阅用户无限次请求 |

详情请参阅 [GitHub Copilot 订阅计划](https://docs.github.com/zh/copilot/about-github-copilot/subscription-plans-for-github-copilot)。

## 技术栈

- [Vite](https://vitejs.dev/) + [React 19](https://react.dev/)
- [vite-plugin-pwa](https://vite-pwa-org.netlify.app/) / Workbox 用于 Service Worker 及离线支持
- 无需后端 — 所有 API 调用均直接在浏览器中发起
